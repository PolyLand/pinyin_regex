# 项目 TODO 列表

> 更新时间: 2026-01-29

## 📌 功能开发 (Features)

## 🐛 Bug 修复 (Bugs)
- [x] **错误处理**: debug_run实现有问题，pattern = "yin" text = "音"都会返回False
- [ ] **错误处理**: 改变控制台编码为 UTF-8，把这一条加到AGENTS.md

## ⚡ 优化/重构 (Optimization)

## 📝 文档/注释 (Documentation)
- [ ] **AGENTS.md修改**: 强调一下这个项目的思路，1.与传统正则不同，会把一个汉字拆为拼音/首字母(可能有多音字)/原始字符实现多匹配；2. 存在双层匹配机制，token["pinyins"]中存储的是拼音/首字母(可能有多音字)/原始字符，token["char"]中存储的是原始字符。在进行常规匹配时使用token["pinyins"]进行匹配，在进行模糊匹配时使用token["char"]进行匹配。

## ✅ 测试 (Tests)
- [ ] **修改测试模式**: 在AGETNTS.md里明确，以后所有的测试样例先生成到一个叫candidates的目录下，由人工审核后再生成测试代码
- [ ] **测试用例**: 添加测试用例，主要针对复杂任务的测试，包括但不限于：1. 多规则组合如："yin(yue|le){2}" 匹配 "音乐了", "(y(yue|le)){2}" 匹配 "音乐"；2. ^$的使用，比如 "^yin(yue|le)$" 匹配 "音乐"；3. .和+是否会错误匹配"⟨BOS⟩", "⟨EOS⟩"